# init

This repository contains the source code for the [init website](https://init.kth.it), built with [Astro](https://astro.build).

## Developing

1. Clone the repository: `git clone git@github.com:itsektionen/init.git`
1. Install dependencies: `npm install`
1. Start the development server: `npm run dev`
